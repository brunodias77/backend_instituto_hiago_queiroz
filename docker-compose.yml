version: '3.8'

services:
  postgres:
    container_name: ong_hiago_queiroz
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=hiago_queiroz